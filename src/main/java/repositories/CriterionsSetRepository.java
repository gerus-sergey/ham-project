package repositories;

import models.CriterionsSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CriterionsSetRepository extends JpaRepository<CriterionsSet, Integer> {
}
