package repositories;

import models.AlternativesSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlternativesSetRepository extends JpaRepository<AlternativesSet, Integer> {
}
